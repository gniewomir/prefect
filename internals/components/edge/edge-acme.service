[Unit]
Description=Prefect Edge ACME (on-demand)
After=edge-pod.service

[Service]
Type=oneshot
# lego per-name bound wait is 120s; allow multi-name want-lists + Edge reload.
TimeoutStartSec=900
ExecStart=/var/lib/host-volume/components/edge/acme-run.sh

[Install]
WantedBy=default.target
